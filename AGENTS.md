# AIエージェントに対する指示書です。

作業時の参照入口は [docs/ai-agent.md](docs/ai-agent.md) です。

## 言葉

応答、コミットメッセージは、すべて日本語でお願いします。
プルリクエストも日本語です。

ブランチの名前は英語にしてください。

## 対象とするファイル

.gitignoreに記載されているファイルは変更してはいけません。
生成物や秘密情報（`.gcloudignore`・`config/*.ini` のうち指示がないもの等）への書き換えは行わないでください。

## 指示に対する動作

指示と関係ない変更を行わないでください。
複数の指示がある場合は、ユーザーが示した順番どおりに進めてください。別件に着手する前に、先に指示された作業を完了してください。
勝手にコミットしないでください。
「ついでにリファクタリングしておきました」というのは困ります。
変更した場合は、作業報告に「人間が動作確認すべき項目」を必ず記載してください。
「コミットして」と指示された場合は、その時点の未コミット変更を原則すべてコミットしてください（別途対象指定がある場合を除く）。
作業の最後には、起動したサーバーを停止し、確認に使ったブラウザページも終了してください。

「提案してください」「考えてください」などと指示されたときは、勝手にコードを変更しないように注意してください。

## コーディング

### コメント

コメントは、コードの動作や意味を書く場所です。

コードの変更内容、変更履歴、変更の目的などは、コメントに書かないでください。
つまり「10pxに変更」「重要な変更」「ここに移動」というようなコメントは禁止します。
しかし「◯◯と◯◯の間を10pxあける」「この関数は、関数◯◯◯よりも前に置く必要がある」というようなコメントは許可します。

コメントや空行は、勝手に削除してはいけません。

### コードのロジック

後方互換性のためのコードを残したり、両対応にしたり、フォールバック処理を作ったりしないでください。
もちろん、明確な指示、または処理ロジックで必要がある場合は、その限りではありません。

### UIの一時メッセージ表示

一時的なメッセージ（読み込み中、保存完了、エラーなど）を表示するために、既存要素の位置が変わる実装は禁止します。
表示位置は、あらかじめ確保した領域に出すか、トーストやオーバーレイを使用してください。

## Git

勝手にコミットしてはいけません。明示的な指示が必要です。

コミットメッセージには、変更の内容のほか、その変更の元になったユーザーの指示も記入してください。
- コミットメッセージは、タイトル（1行目）と本文（2行目以降）からなります。
- タイトルには、変更の内容を簡潔に書いてください。
- 本文には、その変更の元になったユーザーの指示と、詳しい変更の内容を書いてください。
- 「その変更の元になったユーザーの指示」と言っています。「コミットしておいて」などは、変更の元になった指示では無いので、コミットメッセージには含めてはなりません。
- 複数の指示がある場合は箇条書きでまとめます。
- コミットを作成する前に、以上のルールに合致しているかどうか再確認してください。
- ユーザー向けの挙動に影響する変更（機能追加・不具合修正）は、件名を `feat:` または `fix:` で始めてください（リリースニュース抽出用）。

## 仮想環境（必須）

このプロジェクトは、リポジトリ直下のPython仮想環境を前提とします。Python関連のコマンド（`python3`、`pytest`など）を直接または間接的に実行するには、Python仮想環境が有効である
必要があります。

エージェントのセッションの開始時に`which python3`を実行し、結果がプロジェクトルート
の`bin/python3`でない場合には、以後のPython関連コマンドの実行は、bashで
`. bin/activate`の実行後に行う必要があります。

例：
 * grep python *.py    # Python関連コマンドではないので、そのまま実行する。
 * . bin/activate && ./app.py    # `which python3`が/usr/bin/python3などの場合
 * ./app.py    # `which python3`がプロジェクトルートのbin/python3の場合

許可を得ずに仮想環境を新たに作成してはいけません。

### pytest 実行時の注意

テストを実行する場合は、`pytest test` のようにディレクトリ`test`を明示的に指定してください。

## 環境前提条件

- GCP 認証が済んでいること（`gcloud auth login`、適切なプロジェクト選択）。
- ローカルでブラウザ確認を行うときは、`http://127.0.0.1:7000` ではなく `http://localhost:7000` を使うこと。
  - `127.0.0.1:7000` では認証で弾かれることがあります。
- ローカルで人間にブラウザを表示させる必要があるときは、個人用の既存 Chrome プロファイルを使ってはいけません。
  - 原則として、リポジトリ直下の `.chrome` ディレクトリを Chrome の `--user-data-dir` に指定して起動します。
  - 例: `google-chrome-stable --user-data-dir="$PWD/.chrome" --new-window http://localhost:7000/`
- ローカルでブラウザ確認を行うときは、Playwright ではなく Chrome DevTools を使うこと。
  - ヘッドレスではなく、必ずヘッド有りの Chrome を使います。
  - すでに `localhost:7000` を開いている Chrome ウィンドウがある場合は、そのページをそのまま使います。
  - 新しく Chrome を開く必要がある場合も、`.chrome` を `--user-data-dir` に指定します。
- ローカルでのログイン操作は人間が行います。
  - エージェントはログイン画面まで表示し、認証やアカウント選択は人間に委ねてください。
  - 人間にログインさせる必要がある場合は、その旨を明示して待機してください。
- 人間が起動したローカルサーバーや Chrome ウィンドウを、エージェントが勝手に停止・終了してはいけません。
  - 停止してよいのは、エージェント自身が起動したプロセスだけです。
- 作業の最後には、エージェント自身が確認用に起動した Chrome を終了してください。
  - 人間が使っていた既存の Chrome ウィンドウは終了してはいけません。
  - Chrome DevTools 用にエージェント自身が起動した専用プロファイルの Chrome だけを終了対象にします。
- デプロイ先のCloud Runサービス名、リージョン、GCPプロジェクトIDは `.env-deploy` で指定する。
- Cloud Build/Artifact Registry へ push 可能な権限があること。
- Firestore（Native）、履歴用GCSバケット、サイト公開用GCSバケットが存在し、サービスアカウントに権限が付与されていること。
